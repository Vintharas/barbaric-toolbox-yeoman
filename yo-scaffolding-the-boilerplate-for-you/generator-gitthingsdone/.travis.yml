language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-gitthingsdone' ]; then cd .. && eval "mv $currentfolder generator-gitthingsdone" && cd generator-gitthingsdone; fi
